#include <iostream>
using namespace std;
int main()
{
    int arr[];
    cin >> n;
    for (int i = 0, i < n, i++)
    {
        cin >> arr[i];
    }

    for (int i = 0, i < n, i++)
    {
        int tmp;
        tmp = arr[i] arr[i] = arr[n];
        arr[n] = tmp;
    }
     for(int i = 0 , i < n , i++)
     {
         cout<<arr[i];
     }
return 0;
}