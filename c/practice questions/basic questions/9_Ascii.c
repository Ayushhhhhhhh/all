#include <stdio.h>

int main()
{
    char a = '+', b = '-', c = '*', d = '%';

    printf("ASCII value of %c is %d\n", a, a);
    printf("ASCII value of %c is %d\n", b, b);
    printf("ASCII value of %c is %d\n", c, c);
    printf("ASCII value of %c is %d\n", d, d);
    return 0;
}